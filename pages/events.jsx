import * as React from 'react';
import Layout from '../components/MyLayout';
import Events from '../components/events/Events';

// check routing to determine which one to use

export default () => (
  <Layout>
    <Events />
  </Layout>
);
